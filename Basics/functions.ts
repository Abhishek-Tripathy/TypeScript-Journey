// function addTwo (num: number) : number {
//    //return "hello"
//    return num + 2
// }

// const result = addTwo(3)
// console.log(result)

const heros = ["hawkeye", "flash", "magneto"]

const sentence = heros.map((hero) => {return `The hero  name is ${hero}`})

console.log(sentence)